{
  "name": "freef1-server",
  "version": "2.0.0",
  "description": "FreeF1 — live site, visitor tracking, admin dashboard, stream override",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check admin/admin.js && node --check admin/charts.js && node --check admin/analytics.js && node --check bot/discord-bot.js && node --check bot/voice-relay.js && node scripts/smoke-test.js && node scripts/admin-dom-check.js && node scripts/site-dom-check.js && node scripts/relay-audio-check.js",
    "smoke": "node scripts/smoke-test.js",
    "dom": "node scripts/admin-dom-check.js",
    "site": "node scripts/site-dom-check.js",
    "relay": "node scripts/relay-audio-check.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cookie-session": "^2.1.1",
    "discord.js": "^14.27.0",
    "express": "^4.22.2"
  },
  "optionalDependencies": {
    "@discordjs/opus": "^0.10.0",
    "@discordjs/voice": "^0.18.0",
    "ffmpeg-static": "^5.3.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
